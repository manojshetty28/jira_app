function FilterDropdown() {
  return (
    <div className="filter-bar">
      <select className="filter-select" aria-label="Filter by app">
        <option value="">Filter by app</option>
      </select>
    </div>
  )
}

export default FilterDropdown
