# backend_python_app

Placeholder folder for the future Python backend service.

This directory was added on a dedicated feature branch so we can grow the backend in a separate pull request flow without changing the current app behavior yet.
